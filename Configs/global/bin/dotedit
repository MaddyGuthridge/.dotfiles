#!/usr/bin/env bash
# Open dotfiles in VS Code

set -euo pipefail

if command -v zed &> /dev/null; then
    zed -n $DOTFILES
elif command -v zeditor &> /dev/null; then
    zeditor -n $DOTFILES
else
    code -n $DOTFILES
fi
