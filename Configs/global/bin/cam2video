#!/usr/bin/env bash
# Grab input from an attached DSLR over USB, and turn it into a virtual webcam.
gphoto2 --stdout --capture-movie | ffmpeg -i - -vcodec rawvideo -pix_fmt yuv420p -threads 0 -f v4l2 /dev/video4
