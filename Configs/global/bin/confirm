#!/usr/bin/env bash

set -euo pipefail

args="$*"

if [[ -z "$*" ]]; then
    prompt="Continue?"
else
    prompt="$*"
fi

while true; do
    read -p "$prompt (y/n): " yn
    case $yn in
        [Yy]* ) break;;
        [Nn]* ) exit 1;;
        * ) echo "Please answer yes or no.";;
    esac
done
